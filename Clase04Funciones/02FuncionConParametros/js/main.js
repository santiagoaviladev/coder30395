
 //funcion con parámetros

  function sumar(a,b)
  {
    console.log(`SUMA ES ${a+b}`);
  }

   const operando1 = Number(prompt("Ingrese un num"));
   const operando2 = Number(prompt("Ingrese otro num"));

  sumar(operando1, operando2);

  sumar("HOLA", "CACEROLA");